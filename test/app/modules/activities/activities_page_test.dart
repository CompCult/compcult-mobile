import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:museu_vivo/app/modules/activities/activities_page.dart';

void main() {
  testWidgets('ActivitiesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ActivitiesPage(title: 'Activities')));
    //  final titleFinder = find.text('Activities');
    //  expect(titleFinder, findsOneWidget);
  });
}
