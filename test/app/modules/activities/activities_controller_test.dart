import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:museu_vivo/app/modules/activities/activities_controller.dart';
import 'package:museu_vivo/app/modules/activities/activities_module.dart';

void main() {
  initModule(ActivitiesModule());
  // ActivitiesController activities;
  //
  setUp(() {
    //     activities = ActivitiesModule.to.get<ActivitiesController>();
  });

  group('ActivitiesController Test', () {
    //   test("First Test", () {
    //     expect(activities, isInstanceOf<ActivitiesController>());
    //   });

    //   test("Set Value", () {
    //     expect(activities.value, equals(0));
    //     activities.increment();
    //     expect(activities.value, equals(1));
    //   });
  });
}
