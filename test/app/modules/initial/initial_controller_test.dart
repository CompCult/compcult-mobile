import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:museu_vivo/app/modules/initial/initial_controller.dart';
import 'package:museu_vivo/app/modules/initial/initial_module.dart';

void main() {
  initModule(InitialModule());
  // InitialController initial;
  //
  setUp(() {
    //     initial = InitialModule.to.get<InitialController>();
  });

  group('InitialController Test', () {
    //   test("First Test", () {
    //     expect(initial, isInstanceOf<InitialController>());
    //   });

    //   test("Set Value", () {
    //     expect(initial.value, equals(0));
    //     initial.increment();
    //     expect(initial.value, equals(1));
    //   });
  });
}
