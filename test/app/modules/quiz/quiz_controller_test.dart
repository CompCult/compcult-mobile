import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:museu_vivo/app/modules/home/controllers/quiz_controller.dart';
import 'package:museu_vivo/app/modules/quiz/quiz_module.dart';

void main() {
  initModule(QuizModule());
  // QuizController quiz;
  //
  setUp(() {
    //     quiz = QuizModule.to.get<QuizController>();
  });

  group('QuizController Test', () {
    //   test("First Test", () {
    //     expect(quiz, isInstanceOf<QuizController>());
    //   });

    //   test("Set Value", () {
    //     expect(quiz.value, equals(0));
    //     quiz.increment();
    //     expect(quiz.value, equals(1));
    //   });
  });
}
