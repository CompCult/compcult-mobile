import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:museu_vivo/app/modules/home/controllers/game_controller.dart';
import 'package:museu_vivo/app/modules/game/game_module.dart';

void main() {
  initModule(GameModule());
  // GameController game;
  //
  setUp(() {
    //     game = GameModule.to.get<GameController>();
  });

  group('GameController Test', () {
    //   test("First Test", () {
    //     expect(game, isInstanceOf<GameController>());
    //   });

    //   test("Set Value", () {
    //     expect(game.value, equals(0));
    //     game.increment();
    //     expect(game.value, equals(1));
    //   });
  });
}
