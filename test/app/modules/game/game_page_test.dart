import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:museu_vivo/app/modules/home/screens/game_page.dart';

void main() {
  testWidgets('GamePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GamePage(title: 'Game')));
    //  final titleFinder = find.text('Game');
    //  expect(titleFinder, findsOneWidget);
  });
}
