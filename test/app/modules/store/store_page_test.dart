import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:museu_vivo/app/modules/store/store_page.dart';

main() {
  testWidgets('StorePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StorePage(title: 'Store')));
    //  final titleFinder = find.text('Store');
    //  expect(titleFinder, findsOneWidget);
  });
}
