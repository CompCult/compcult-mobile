import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:museu_vivo/app/modules/home/screens/mission_page.dart';

void main() {
  testWidgets('MissionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MissionPage(title: 'Mission')));
    //  final titleFinder = find.text('Mission');
    //  expect(titleFinder, findsOneWidget);
  });
}
