import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:museu_vivo/app/modules/home/controllers/mission_controller.dart';
import 'package:museu_vivo/app/modules/mission/mission_module.dart';

void main() {
  initModule(MissionModule());
  // MissionController mission;
  //
  setUp(() {
    //     mission = MissionModule.to.get<MissionController>();
  });

  group('MissionController Test', () {
    //   test("First Test", () {
    //     expect(mission, isInstanceOf<MissionController>());
    //   });

    //   test("Set Value", () {
    //     expect(mission.value, equals(0));
    //     mission.increment();
    //     expect(mission.value, equals(1));
    //   });
  });
}
