import 'package:flutter/material.dart';

class QuizSubmit extends StatelessWidget {
  static const String routeName = '/quiz-submission';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Quiz submit'),
      ),
    );
  }
}
